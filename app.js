const express = require('express'); // importer express avec require 

const app = express(); // on appelle la méthode express

module.exports = app; // on export cette constante pour y accéder depuis nos autres fichers. 
